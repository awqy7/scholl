# ARIA

Sistema de gestão escolar com inteligência artificial (Groq). Cadastros, grade horária, faltas, substituições e conversa natural com o diretor.

## Desenvolvimento

```bash
npm install
npm run dev
```

Configure `.env.local` (veja `.env.local.example`).

## Teste da IA

```bash
npm run test:ia
```